name: srikandisehat
description: "Srikandi Sehat App"
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  flutter_dotenv: ^6.0.0
  shared_preferences: ^2.5.3
  provider: ^6.1.5+1
  fl_chart: ^1.1.1
  table_calendar: ^3.2.0
  intl: ^0.20.2
  open_file: ^3.5.10
  path_provider: ^2.1.5
  device_preview: ^1.3.1
  url_launcher: ^6.3.2
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  cloud_firestore: ^6.0.2
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
